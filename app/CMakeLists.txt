# ============================================================
#  app/ — UI code (depends on third_imgui)
# ============================================================
add_subdirectory(shell)
# panels added in later tasks
